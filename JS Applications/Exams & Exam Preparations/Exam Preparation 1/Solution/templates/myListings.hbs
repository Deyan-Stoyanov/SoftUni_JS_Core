{{> header}}
<div class="my-listings">
    <h1>My car listings</h1>
    <div id="listings">
        {{#each listings}}
            {{#if isAuthor}}
                {{> listing}}
            {{/if}}
        {{else}}
            <p class="no-cars">No cars in database.</p>
        {{/each}}
    </div>
</div>
{{> footer}}
